# Minimal Case

This fixture is intentionally tiny. It verifies that the stable fork can load a
case, preserve run metadata, and write report/metric artifacts without using a
paid LLM call.

Question: Which option is better supported by the seed evidence?

Expected answer shape: structured report plus metrics JSON.
