# IPC Topic Packet

Topic: Argentina IPC 2025 forecast.

Source basis: PR #22 / `origin/feat/case-b-backtesting`.

Important difference from football/Bolivia:

- The IPC prior PR used a larger input pack, not scheduled injection.
- This S3 packet uses a reduced baseline plus injected evidence packets so scheduled injection has a real experimental role.
- Off-topic IPC noise was not present as a clean source in PR #22, so `noise_off_topic.md` is an explicit synthetic distractor control.

